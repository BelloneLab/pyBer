name: pyBer
channels:
  - conda-forge
  - defaults

dependencies:
  # Core interpreter
  - python=3.11

  # Numeric + signal processing
  - numpy>=1.24
  - scipy>=1.10
  - pyqtgraph>=0.13
  # File IO (Doric H5)
  - h5py>=3.9

  # Baseline correction
  - pybaselines>=1.1

  # GUI
  - pyside6>=6.6


  - scikit-learn>=1.3

  # Quality-of-life / packaging
  - pip
  - setuptools
  - wheel


  - matplotlib>=3.8


  - pandas>=2.0
  - pyinstaller>=6.0
  - pyinstaller-hooks-contrib>=2024.0
